================================================================================
ordinary strings reject newlines
================================================================================
use import
import "first
second"
caveat invalid(value string) { value == "first
second" }
--------------------------------------------------------------------------------

(source_file
  (use_flag
    name: (identifier))
  (ERROR
    (UNEXPECTED '\n')
    (UNEXPECTED 'v')
    (UNEXPECTED 'v')
    (UNEXPECTED '\n')))
